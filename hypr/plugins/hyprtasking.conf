# hyprlang noerror true

bind = SUPER, grave, hyprtasking:toggle, cursor
bind = SUPER SHIFT, grave, hyprtasking:toggle, all

plugin {
  hyprtasking {
    layout = grid
    gap_size = 0
    bg_color = rgb(0F0F0F)
    border_size = 0
    exit_behavior = hovered
  }
}

# hyprlang noerror false
